{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run cmd",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}/cmd",
            "cwd": "${workspaceRoot}",
            "envFile": "${workspaceRoot}/.env"
        },
    ]
}